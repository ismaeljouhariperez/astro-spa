---
import Layout from '../layouts/Layout.astro';
import InteractiveCounter from '../components/InteractiveCounter';
---

<Layout>
  <div class="min-h-screen bg-gradient-to-b from-blue-50 to-white">
    <header class="bg-blue-800 text-white shadow-md">
      <div class="container mx-auto py-4 px-6 flex justify-between items-center">
        <h1 class="text-2xl font-bold">Astro + React SPA</h1>
        <nav>
          <ul class="flex space-x-6">
            <li><a href="#hero" class="hover:text-blue-200 transition-colors">Accueil</a></li>
            <li><a href="#features" class="hover:text-blue-200 transition-colors">Fonctionnalités</a></li>
            <li><a href="#interactive" class="hover:text-blue-200 transition-colors">Démo</a></li>
            <li><a href="#faq" class="hover:text-blue-200 transition-colors">FAQ</a></li>
          </ul>
        </nav>
      </div>
    </header>

    <main>
      <!-- Hero Section -->
      <section id="hero" class="py-16 px-4">
        <div class="container mx-auto text-center">
          <h2 class="text-4xl md:text-5xl font-extrabold text-blue-900 mb-4">
            Développez des SPAs performantes avec Astro et React
          </h2>
          <p class="text-lg md:text-xl text-gray-700 max-w-3xl mx-auto mb-8">
            Astro permet de créer des sites rapides en utilisant vos 
            frameworks préférés comme React, tout en n'envoyant que le minimum 
            de JavaScript au navigateur.
          </p>
          <div class="flex justify-center space-x-4">
            <a href="#interactive" class="bg-blue-700 hover:bg-blue-800 text-white font-semibold px-6 py-3 rounded-lg shadow transition-colors">
              Voir la démo
            </a>
            <a href="https://docs.astro.build" target="_blank" rel="noopener" class="bg-white hover:bg-gray-100 text-blue-700 font-semibold px-6 py-3 rounded-lg shadow border border-blue-200 transition-colors">
              Documentation
            </a>
          </div>
        </div>
      </section>

      <!-- Features Section -->
      <section id="features" class="py-16 px-4 bg-blue-50">
        <div class="container mx-auto">
          <h2 class="text-3xl font-bold text-center text-blue-900 mb-12">Pourquoi utiliser Astro avec React ?</h2>
          
          <div class="grid md:grid-cols-3 gap-8">
            <div class="bg-white p-6 rounded-lg shadow-md">
              <div class="text-blue-600 text-4xl mb-4">🚀</div>
              <h3 class="text-xl font-semibold text-blue-800 mb-2">Performance exceptionnelle</h3>
              <p class="text-gray-700">Astro n'envoie que le JavaScript nécessaire au client, ce qui se traduit par des sites plus rapides.</p>
            </div>
            
            <div class="bg-white p-6 rounded-lg shadow-md">
              <div class="text-blue-600 text-4xl mb-4">⚛️</div>
              <h3 class="text-xl font-semibold text-blue-800 mb-2">React où vous en avez besoin</h3>
              <p class="text-gray-700">Utilisez React uniquement pour les parties interactives, pas pour tout le site.</p>
            </div>
            
            <div class="bg-white p-6 rounded-lg shadow-md">
              <div class="text-blue-600 text-4xl mb-4">🧩</div>
              <h3 class="text-xl font-semibold text-blue-800 mb-2">Facile à apprendre</h3>
              <p class="text-gray-700">Vous connaissez déjà React ? Vous êtes déjà à mi-chemin de la maîtrise d'Astro !</p>
            </div>
          </div>
        </div>
      </section>

      <!-- Interactive Demo Section -->
      <section id="interactive" class="py-16 px-4">
        <div class="container mx-auto">
          <h2 class="text-3xl font-bold text-center text-blue-900 mb-8">Démo Interactive</h2>
          <p class="text-center text-lg text-gray-700 max-w-3xl mx-auto mb-8">
            Ce composant React est hydraté côté client, permettant une interactivité complète
            sans charger React sur toute la page.
          </p>
          
          <div class="max-w-md mx-auto">
            <InteractiveCounter client:load />
          </div>
        </div>
      </section>

      <!-- FAQ Section -->
      <section id="faq" class="py-16 px-4 bg-blue-50">
        <div class="container mx-auto">
          <h2 class="text-3xl font-bold text-center text-blue-900 mb-12">Questions fréquentes</h2>
          
          <div class="max-w-3xl mx-auto space-y-6">
            <div class="bg-white p-6 rounded-lg shadow-md">
              <h3 class="text-xl font-semibold text-blue-800 mb-2">Qu'est-ce qu'Astro exactement ?</h3>
              <p class="text-gray-700">Astro est un framework web moderne qui permet de créer des sites rapides avec moins de JavaScript. Il vous permet d'utiliser vos frameworks UI préférés comme React, mais ne les envoie au navigateur que lorsque c'est nécessaire.</p>
            </div>
            
            <div class="bg-white p-6 rounded-lg shadow-md">
              <h3 class="text-xl font-semibold text-blue-800 mb-2">Comment Astro et React fonctionnent-ils ensemble ?</h3>
              <p class="text-gray-700">Astro vous permet d'utiliser des composants React à l'intérieur de vos pages Astro. Vous pouvez contrôler quand et comment React est hydraté côté client avec des directives comme client:load, client:visible, etc.</p>
            </div>
            
            <div class="bg-white p-6 rounded-lg shadow-md">
              <h3 class="text-xl font-semibold text-blue-800 mb-2">Puis-je créer une vraie SPA avec Astro ?</h3>
              <p class="text-gray-700">Oui ! Avec l'intégration @astrojs/react, vous pouvez créer une application React complète dans Astro. Vous pouvez également utiliser des bibliothèques comme React Router pour une navigation côté client.</p>
            </div>
          </div>
        </div>
      </section>
    </main>

    <footer class="bg-blue-900 text-white py-8 px-4">
      <div class="container mx-auto">
        <div class="flex flex-col md:flex-row justify-between items-center">
          <div class="mb-4 md:mb-0">
            <h2 class="text-xl font-bold">Astro + React SPA</h2>
            <p class="text-blue-200">La combinaison parfaite pour les sites modernes</p>
          </div>
          <div>
            <p>&copy; 2024 Astro SPA Example. Tous droits réservés.</p>
          </div>
        </div>
      </div>
    </footer>
  </div>
</Layout>

<script>
  // Script pour la navigation smooth
  document.addEventListener('DOMContentLoaded', () => {
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        
        const targetId = this.getAttribute('href').substring(1);
        const targetElement = document.getElementById(targetId);
        
        if (targetElement) {
          window.scrollTo({
            top: targetElement.offsetTop - 80,
            behavior: 'smooth'
          });
          
          history.pushState(null, null, `#${targetId}`);
        }
      });
    });
  });
</script> 